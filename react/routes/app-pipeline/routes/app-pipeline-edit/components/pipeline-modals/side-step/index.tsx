import React from 'react';

import './index.less';

const prefix = 'c7ncd-sideStep';

const Index = () => (
  <div className={prefix}>
    <div className={`${prefix}__item`}>
      <p className={`${prefix}__item__line`} />
      <p className={`${prefix}__item__text`}>基础步骤</p>
    </div>
  </div>
);

export default Index;
