import React from 'react';

const prefixCls = 'c7ncd-pipeline-jobItem';

const SerialLines = () => (
  <div className={`${prefixCls}-serialLines`}>
    {/* <div className={`${prefixCls}-serialLines-line`} /> */}
    <span className={`${prefixCls}-circle`} />
  </div>
);

export default SerialLines;
